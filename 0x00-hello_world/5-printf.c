#include <stdio.h>

/**
 *main - this function uses puts to print
 *
 *Desscription: it print the statement
 *with proper grammar, but the outcome is a piece of art,
 *
 * Return: 0
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,");
	return (0);
}

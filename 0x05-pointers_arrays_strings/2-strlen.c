#include "main.h"
#include <string.h>

/**
 * _strlen - this function print the length te of a character
 *
 * @s: the parameter for the function
 *
 * Return: always 0
 */

int _strlen(char *s)
{
return (strlen(s));
}

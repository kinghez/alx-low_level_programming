#include "main.h"

/**
 * _islower - a function that check in a number is lower
 *@c: is int that will be use for the arhument of the function
 * Return: 0
 */
int _islower(int c)
{
if (c >= 'a' && c <= 'z')
{
return (1);
}
else
{
return (0);
}
}
